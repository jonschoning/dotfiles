 if(interactive()){
     options(vimcom.verbose = 1)
     library(vimcom)
     library(ggplot2)
     library(lattice)
     library(reshape2)
 }
options(repos=structure(c(CRAN="http://ftp.ussg.iu.edu/CRAN/")))
